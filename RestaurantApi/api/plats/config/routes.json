{
  "routes": [
    {
      "method": "GET",
      "path": "/plats",
      "handler": "Plats.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/plats/count",
      "handler": "Plats.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/plats/:id",
      "handler": "Plats.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/plats",
      "handler": "Plats.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/plats/:id",
      "handler": "Plats.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/plats/:id",
      "handler": "Plats.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
